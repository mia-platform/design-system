import {Themes} from '../../src/utils/theme'

/**
 * The backgrounds switcher configuration for Storybook.
 */
const backgrounds = {
  default: Themes.light,
  grid: { 
    disable: true 
  }
}

export default backgrounds
